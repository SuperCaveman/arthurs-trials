# Implementation status and evidence

This document separates the working Arthur's Trials proof from the planned
production architecture. It is intentionally conservative: a planned component
is never described as deployed or tested.

Last reviewed: 2026-08-14

| Capability | Status | Evidence | Next proof |
| --- | --- | --- | --- |
| Unreal dedicated server | **Working locally** | A Win64 dedicated server runs headlessly and accepts a local client on the Third Person map. | Repeat the local test after each server change. |
| GameLift Servers Anywhere lifecycle | **Working locally** | The server has completed `InitSDK`, `ProcessReady`, health checks, game-session activation, and graceful `ProcessEnding`/`Destroy` against the real Anywhere fleet. | Capture a short, redacted lifecycle video and server-log excerpt. |
| GameLift health fault injection | **Verified locally** | The Anywhere launcher can opt in to one bounded failed health check with `-FailHealthChecks`. GameLift issued `TerminateProcess` for the unhealthy process; a clean replacement process then completed `ProcessReady` and returned healthy. It is disabled by default and guarded by a static CI contract test. | Repeat the controlled failure and replacement-process recovery test after lifecycle changes. |
| Game-session lifecycle | **Working locally** | The helper scripts create an Anywhere game session and terminate it through the GameLift termination callback. | Document session identifiers only in redacted evidence. |
| FlexMatch matchmaking | **Rule-set template validated locally; not deployed** | A versioned two-to-four player co-op FlexMatch template encodes party-safe latency limits, timed relaxation, and backfill preference. It is JSON/static-contract validated only; no matchmaker, queue, fleet, or ticket exists. See the [design](FLEXMATCH_DESIGN.md). | Submit real latency-aware tickets through the authenticated session API during an explicitly approved managed demo. |
| GameLift placement and capacity | **Queue/scaling templates validated locally; not deployed** | Placeholder-safe game-session queue and rule-based scaling policy templates encode latency-first placement, bounded latency relaxation, a 25% available-session scale-out buffer, and deliberately slow scale-in. No alias, queue, fleet, or scaling policy exists. See the [capacity design](GAMELIFT_CAPACITY_PLACEMENT.md). | Use a revisioned fleet alias and measured host capacity to run a time-boxed placement/scale test. |
| GameLift capacity model | **Local planning tool validated; not deployed** | A deterministic generator turns player concurrency plus measured server inputs into active sessions, spare session buffer, required instances, and explicit max-capacity warnings. Its committed values are marked assumptions, not benchmark data. See the [capacity model](GAMELIFT_CAPACITY_MODEL.md). | Replace assumptions with managed Linux soak-test measurements before choosing live fleet capacity. |
| Player-session admission | **Verified locally** | The linked dedicated server accepted a real GameLift `PlayerSessionId` with `AcceptPlayerSession`, bound it to the player, and rejected a separate client without a credential during `PreLogin`. See [redacted proof](evidence/LOCAL_PLAYER_SESSION_PROOF.md). | Replace the local helper with the planned authenticated session API. |
| Client credential boundary | **Verified locally** | `New-GameLiftAnywherePlayerSession.ps1` returned only connection details plus a short-lived player-session credential; the Unreal client received no AWS credentials. See [redacted proof](evidence/LOCAL_PLAYER_SESSION_PROOF.md). | Replace the local helper with the planned authenticated session API. |
| Player authentication | **Cognito-compatible verifier tested locally; managed foundation created and torn down** | The opt-in Cognito user-pool/public-client Terraform module keeps the zero-resource default plan empty. During a time-boxed demo it created a live player pool and public Unreal app client with no client secret, then tore both down. The session API verifies locally generated Cognito-shaped RS256 access tokens against an injected JWKS; no live sign-in was performed. See [managed-demo evidence](evidence/MANAGED_FOUNDATION_DEMO.md) and the [security/delivery foundation](SECURITY_DELIVERY_FOUNDATION.md). | Run an explicitly approved live Cognito sign-in and verify a client cannot request another player's session. |
| Session API | **Verified locally and in one managed client-join proof; not deployed** | A Node HTTP API enforces a local caller boundary, party ownership, idempotency, owner-only reads, and returns only connection details plus a player-session credential. Its Anywhere adapter created a real local GameLift session and credential that the dedicated server accepted. During one time-boxed managed proof, the default-off managed-fleet adapter created a real GameLift session and returned the credential used by a staged Unreal client to join the managed server; it cannot create a fleet or image. The separate queue adapter models `StartGameSessionPlacement` with complete party latency and returns only the caller's reservation from a fulfilled placement; it is mock-tested, not live. The API uses the official AWS SDK rather than a bundled AWS CLI, so a future ECS task can use its task role. It maps direct GameLift capacity exhaustion to a player-safe `409 PLACEMENT_PENDING` response. An optional atomic file store proves one restart-safe idempotency path, but is intentionally single-process and not RDS. See [redacted proof](evidence/SESSION_API_ANYWHERE_PROOF.md), [managed container evidence](evidence/MANAGED_GAMELIFT_CONTAINER_DEMO.md), and the [database foundation](DATABASE_FOUNDATION.md). | Add transactional RDS persistence, Cognito live sign-in, least-privilege ECS task role, and approved queue-placement test. |
| Session API container | **Verified locally** | The local image builds successfully, runs as a non-root user, and returns `ok` from `/healthz`. Its default adapter is fake, so the container test has no AWS dependency. CI will build the image after the first repository push. | Keep its release discipline aligned with the verified Linux dedicated-server image. |
| Results-worker container | **Verified locally; managed runtime template validated; not deployed** | The idempotent Node worker has a non-root container artifact, SQS consumer entry point, PostgreSQL receipt-and-reward transaction adapter, and migration. The default local container proof remains isolated from AWS. The default-off Terraform service uses private subnets, no ingress, narrow SQS/secret access, 14-day logs, and desired count zero. | Push an immutable image, choose a private-egress strategy, run the migration with a narrower DB role, then perform an explicitly approved live demo. |
| Session API local benchmark | **Verified locally** | The fake-adapter test completed 40/40 requests at concurrency 10 with 26.59 ms p95 local HTTP latency. See [scoped evidence](evidence/SESSION_API_LOCAL_BENCHMARK.md). | Run a separate Unreal/server soak test; do not extrapolate API overhead to player capacity. |
| Bounded placement/load simulation | **Verified locally; synthetic only** | A capped local simulator generates per-request CSV, summary JSON, and an SVG chart for session-placement, admission, and completion timing. Its fake adapter never calls AWS or launches Unreal, and its outputs are labelled accordingly. See the [simulation guide](LOCAL_PLACEMENT_SIMULATION.md). | Replace the synthetic timing with a separately approved managed queue-placement and Linux-server soak test. |
| Linux dedicated-server artifact | **Verified locally and in one managed GameLift session** | UE 5.8's v26 Linux cross-toolchain produced a 64-bit x86_64 ELF server. The server was cooked, staged, packed with IoStore, archived, built into a lean Amazon Linux 2023 runtime image that excludes `.debug` and `.sym` files, and smoke-tested as a non-root process bound to UDP `7777`. A capped one-vCPU/2-GiB startup sample remained healthy at 108.4 MiB; this is a baseline, not a capacity claim. A revision-tagged image subsequently reached `ACTIVE` in one time-boxed managed GameLift container fleet and hosted one game session. See the [managed-demo evidence](evidence/MANAGED_GAMELIFT_CONTAINER_DEMO.md). | Run a separately approved managed Linux soak test before making capacity claims. |
| GameLift managed-container definition | **Verified in a time-boxed managed demo; torn down** | A GameLift container-group definition used Server SDK 5.6.0, Amazon Linux 2023, UDP `7777-7779`, one vCPU, and 2048 MiB. The first image exposed a missing GameLift lifecycle launch switch; the corrected immutable image reached `ACTIVE` and hosted one session on a one-instance `c5.large` fleet. Container definitions were deleted after capture. See [managed-demo evidence](evidence/MANAGED_GAMELIFT_CONTAINER_DEMO.md). | Repeat only with an explicit short-lived budget and measure real session load before increasing capacity. |
| Match results workflow | **Verified locally; SQS/DLQ foundation created and torn down** | A real Anywhere game session carried server-only match metadata. The dedicated server published `match.completed` to a local outbox; the worker processed XP exactly once and archived the event. An opt-in local file store proves the same event is rejected after a worker restart while preserving original rewards. A time-boxed AWS demo created encrypted SQS/DLQ queues, completed a synthetic round trip, and verified poison-message redrive before teardown. RDS/ECS remain template-only. See [managed-demo evidence](evidence/MANAGED_FOUNDATION_DEMO.md), [redacted local proof](evidence/LOCAL_MATCH_RESULTS_PROOF.md), and the [async foundation](ASYNC_RESULTS_FOUNDATION.md). | Run a time-boxed, explicitly approved SQS/RDS/ECS test after choosing private egress and applying the schema with a narrow DB role. |
| Results-worker resilience | **Verified locally; SQS semantics simulated** | A bounded in-memory simulation exercises the real worker-consumer decision path: one transient failure is retried and processed once, while one malformed message exhausts three deliveries and lands in a simulated DLQ. No SQS, ECS, or RDS resource is created. See the [resilience guide](RESULTS_WORKER_RESILIENCE.md). | Confirm the same disposition sequence during an explicitly approved SQS/DLQ demo. |
| Terraform security foundation | **Validated locally; managed VPC/Cognito/SQS window created and torn down** | The `infra/` root has a zero-resource local default. The VPC and Cognito identity modules are opt-in, require an explicit demo flag and expiry tag, and deliberately avoid a NAT gateway. A one-window deployment created 17 low-cost VPC/Cognito/SQS resources, validated encryption and DLQ behavior, and destroyed all 17 with an empty Terraform state. No IAM role, ECS, RDS, or managed fleet was created. See [managed-demo evidence](evidence/MANAGED_FOUNDATION_DEMO.md). | Add remaining demo modules only for a short, tagged demo window. |
| Multiplayer threat model | **Documented; controls evidence-bound** | The threat model maps dedicated-server, session, results, and data trust boundaries to locally verified, template-validated, and planned controls without describing any undeployed security service as active. See the [threat model](THREAT_MODEL.md). | Review with a real team/security owner before a production deployment. |
| CI quality gate | **Verified remotely; no-deploy release candidate executed** | The public GitHub Actions workflow tests API/worker behavior, validates image/IaC contracts, builds both local containers, and proves the zero-resource plan. The manual release-candidate workflow also passed on 2026-08-13: it tested both packages, built two candidate images, and uploaded a short-lived manifest with two local artifact hashes and `deployment.performed: false`. It used no AWS credential, registry login, or deploy path. See the [release workflow](RELEASE_CANDIDATE_WORKFLOW.md). | Add a separately protected, least-privilege image-publish/deployment workflow only for an approved cloud-test budget. |
| Managed hosting | **Managed GameLift API-to-client proof completed; torn down** | A private ECR image, GameLift service role, Linux container group, one-instance `c5.large` managed container fleet, and one active game session were created for a recorded proof. The server lifecycle launch configuration was corrected during the test, then player-session validation was enabled. A staged Unreal client joined using a credential issued by the managed-fleet API adapter. The container definitions, ECR repository/images, and role were deleted afterward; the fleet had zero remaining capacity while its control-plane deletion finalized. See the [container preflight](MANAGED_GAMELIFT_CONTAINER_PREP.md) and [sanitized managed evidence](evidence/MANAGED_GAMELIFT_CONTAINER_DEMO.md). | Run a separately approved, tightly bounded session-load and scale test; do not keep a managed fleet running between demos. |
| Observability / dashboards | **Local dashboard verified; CloudWatch template validated; not deployed** | A sanitized static dashboard derives real GameLift lifecycle, authoritative result, worker-processing, and graceful-shutdown evidence from local logs/outbox data. Default-off Terraform now models one CloudWatch dashboard and five alarms covering queue age, DLQ depth, ECS worker CPU/memory, and RDS free storage. Its alarm actions are empty unless an approved existing destination is supplied. See the [dashboard guide](LOCAL_OPERATIONS_DASHBOARD.md). | Add structured logs, choose approved alarm routing, and verify a controlled cloud failure during a time-boxed managed demo. |
| Virtual-production exploration | **Parked future extension** | Earlier research and local proof scripts are preserved in the repository but excluded from the active game-development portfolio architecture, case study, capture plan, and AWS demo plan. No virtual-production AWS resources are deployed. See the [scope note](PARKED_FUTURE_WORK.md). | Resume only after an explicit future scope decision. |

## Current low-cost operating mode

- The active development proof uses a GameLift Servers Anywhere fleet with the
  workstation registered as its compute; it does not create managed EC2 capacity.
- Managed-cloud infrastructure is intentionally off.
- Local sessions are short-lived and are terminated with
  `Stop-GameLiftAnywhereSession.ps1` after each test.
- Player-session admission has been runtime-verified with both accepted and
  rejected local join attempts; the redacted evidence is stored with this
  project.

## Evidence standard for the public portfolio

Every public claim should be backed by at least one of these:

1. a repeatable script and sanitized output;
2. a focused server-log excerpt with identifiers redacted;
3. an architecture or decision record labelled as planned; or
4. a short screen recording of the exact tested flow.

Do not present the planned ECS, RDS, SQS, Terraform, FlexMatch, or multi-region
design as deployed until it has real, reproducible evidence.
