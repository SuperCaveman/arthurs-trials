import { mkdir, readFile, writeFile } from 'node:fs/promises';
import { dirname } from 'node:path';

function escapeHtml(value) {
  return String(value).replace(/[&<>'"]/g, (character) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;' })[character]);
}

export function renderWorkflowDashboard(workflow) {
  const transitions = workflow.transitions.map((transition, index) => `<article class="step"><span class="number">${index + 1}</span><div><h2>${escapeHtml(transition.status)}</h2><p>${escapeHtml(transition.actor)}</p></div></article>`).join('');
  const mappings = Object.entries(workflow.productionMapping).map(([step, service]) => `<li><strong>${escapeHtml(step)}</strong><span>${escapeHtml(service)}</span></li>`).join('');
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Castle Set v12 — Virtual Production Workflow</title><style>:root{color-scheme:dark;--ink:#edf5ff;--muted:#afc2d6;--line:#294766;--panel:#102438;--blue:#61b8ff;--green:#66e3ae;background:#06121e}*{box-sizing:border-box}body{margin:0;background:radial-gradient(circle at 74% 0,#19466c,transparent 34rem),#06121e;color:var(--ink);font:16px/1.45 Inter,Segoe UI,sans-serif}.wrap{max-width:1120px;margin:auto;padding:48px 28px 68px}.eyebrow{color:var(--green);font-weight:700;letter-spacing:.13em;text-transform:uppercase;font-size:.78rem}h1{font-size:clamp(2.2rem,6vw,4.6rem);line-height:1;margin:.35rem 0 1rem}p{color:var(--muted)}.badges{display:flex;flex-wrap:wrap;gap:.55rem;margin:1.4rem 0 2rem}.badge{border:1px solid var(--line);border-radius:999px;padding:.38rem .7rem;color:var(--muted);font-size:.86rem}.good{border-color:#2d8c68;color:var(--green)}.steps{display:grid;grid-template-columns:repeat(5,1fr);gap:10px}.step,.card{border:1px solid var(--line);background:linear-gradient(145deg,#12314b,#0c1e31);border-radius:16px;padding:18px}.number{display:grid;place-items:center;width:32px;height:32px;border-radius:50%;background:#1c5a87;color:#fff;font-weight:800}.step h2{font-size:1.05rem;margin:.8rem 0 .2rem}.step p{font-size:.85rem;margin:0}.grid{display:grid;grid-template-columns:1.15fr .85fr;gap:14px;margin-top:14px}.card h2{font-size:.9rem;letter-spacing:.09em;text-transform:uppercase;color:var(--muted);margin:0 0 .8rem}.card strong{display:block;font-size:1.5rem}.card ul{list-style:none;padding:0;margin:0}.card li{padding:.6rem 0;border-top:1px solid var(--line);display:grid;gap:.1rem}.card li:first-child{border-top:0;padding-top:0}.card li span{color:var(--muted)}.foot{font-size:.86rem;margin-top:16px}@media(max-width:850px){.steps{grid-template-columns:1fr}.grid{grid-template-columns:1fr}}</style></head><body><main class="wrap"><div class="eyebrow">Arthur's Trials · Unreal Engine Cloud Platform</div><h1>${escapeHtml(workflow.versionId)}<br>virtual-production workflow</h1><p>Remote Unreal content evolves through an approved stage-delivery path while latency-sensitive rendering remains local.</p><div class="badges"><span class="badge good">Local workflow verified</span><span class="badge good">Approved for stage</span><span class="badge">No AWS resources deployed</span><span class="badge">Cloud architecture mapped</span></div><section class="steps">${transitions}</section><section class="grid"><div class="card"><h2>Asset version</h2><strong>${escapeHtml(workflow.asset.package)}</strong><p>Manifest SHA-256 recorded · ${escapeHtml(workflow.asset.estimatedBytes)} planned bytes</p><p>Target: ${escapeHtml(workflow.stageTarget)}</p></div><div class="card"><h2>Production AWS mapping</h2><ul>${mappings}</ul></div></section><p class="foot">${escapeHtml(workflow.scope)}</p></main></body></html>`;
}

export async function createWorkflowDashboard({ workflowPath, outputPath }) {
  const workflow = JSON.parse(await readFile(workflowPath, 'utf8'));
  await mkdir(dirname(outputPath), { recursive: true });
  await writeFile(outputPath, renderWorkflowDashboard(workflow), 'utf8');
  return workflow;
}
